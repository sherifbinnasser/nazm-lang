[package]
name = "nazmc_data_pool"
version = "0.1.0"
edition = "2021"

[dependencies]
typed-index-collections = "*"
derive_more = { version = "*", features = ["from", "into"] }
nazmc_diagnostics = { path = "../nazmc_diagnostics" }